import React from "react";

const UnknownStatusError: React.FC = () => {
    return (
        <div>
            <p>{"Oops... something went wrong, try again 🤕"}</p>
        </div>
    );
};

export default UnknownStatusError;